<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to 'Tutoriapps', root_path, :class => 'brand' %>
      <div class="brand"><span class="slogan"><%= t 'slogan' %></span></div>
      <% if user_signed_in? %>
        <a class="btn btn-navbar" data-toggle="collapse"
        data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <div class="nav-collapse">
          <ul class="nav pull-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to I18n.t('my_account'), edit_user_registration_path %></li>
                <li><%= link_to I18n.t('helpers.sign_out'), destroy_user_session_path,
                  :method => :delete %></li>
              </ul>
            </li>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</div>